<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- THE plugin app is the only component in the Kenosis ecosystem that
         touches the network. The Kenosis host (hr.exel.kenosis_ai) keeps its
         hard no-INTERNET invariant (the architecture decision record, CI-enforced by
         the permission-check CI script); it reaches this app over LOCAL binder
         only (AIDL IKenosisPlugin). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="Kenosis AI - Internet Search plugin"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- The plugin service the host binds. exported="true" (the host is a
             different app) + the PLUGIN_SERVICE intent action the host's
             <queries> block declares. Bound service only — no start, no
             foreground type. The tool manifest is NOT in <meta-data>: it is
             served by getManifest() over binder (one representation). -->
        <service
            android:name=".InternetPluginService"
            android:exported="true">
            <intent-filter>
                <action android:name="hr.exel.kenosis_ai.PLUGIN_SERVICE"/>
            </intent-filter>
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>